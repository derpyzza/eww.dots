(include "variables.yuck")
(include "widgets.yuck")

(defwindow bar
          :monitor 0
          :geometry (geometry 
                      :x "0px"
                      :y "0px"
                      :width "3%"
                      :height "100%"
                      :anchor "center right")
          :reserve (struts :distance "3%" :side "right")
          :stacking "fg"
          :windowtype "dock"
          :wm-ignore "false"
      (bar))
