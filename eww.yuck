(include "variables.yuck")
(include "widgets.yuck")

(defwindow button
          :monitor 0
          :geometry (geometry 
                      :x "8px"
                      :y "0px"
                      :width "3%"
                      :height "100%"
                      :anchor "center right")
          :stacking "fg"
          :windowtype "dock"
          :wm-ignore "false"
      (bar))
